# Phase 4 — Metadata & Map

2026-09-10。已完成本阶段修复与 Chromium 验收；保持现有视觉、Photo Engine、Project System 和 Manifest v10，不新增正式 Project、不修改照片仓库、不部署。

## 本次修复

- **真实 GPS 漏显**：154 张照片的 `location` 均为空，但其中 139 张已有 EXIF GPS。UI 现在可直接使用原生数字坐标和半球参考，地图与 Metadata 保持一致；不推测地名，不为其余 15 张补坐标。
- **构建时间误作拍摄时间**：`result_final_01.jpg` 没有 EXIF 拍摄时间，原生 `dateTaken` 实为构建时钟。现在显示“未记录”，日期筛选不包含它、时间排序将其置末。其他 153 张保留 EXIF 时间、毫秒和偏移；时区来源按详细信息加载，不转换为浏览器时区。
- **单位与缺失值**：实际焦距与 35mm 等效分开，补齐快门秒、曝光补偿 EV、海拔米和角度单位；二进制文件大小标为 KiB/MiB。零海拔、零曝光补偿、零坐标保留；缺失/非法数据不补造。Engine 未保留焦平面分辨率单位，明确标为原始值。
- **联动与焦点**：筛选、排序和地图返回上下文写入 URL，分享/刷新恢复同一 Viewer 顺序；修复 Forward 后关闭产生重复历史、从地图看图后丢失返回位置的问题。当前页面保留地图视角。元数据切图清除旧详情；修复详情重试完成后方向键因焦点丢失而失效。
- **失败回退**：地图模块下载失败也提供可选照片列表；地图加载超时/GPU 上下文丢失有失败提示，异步展开在组件销毁后不再操作地图。
- **公开产物边界**：原先 Astro 会复制全部缩略图，包括未公开照片。新增网站构建收尾，仅清理输出目录中的未引用照片资产；验证 draft 图片、缩略图和越项目详情 URL 均为 404。正式空站构建输出 0 张缩略图。

## 已有功能的验证结果

**真实数据**：复用已有 Manifest 和缩略图的隔离预览，不创建正式选集文件。逐项核对全部 154 张的日期/曝光/GPS 投影；目视抽查首个 24 张预览选集的 1440×1000 桌面与 390×844 移动布局、Viewer 和信息抽屉，无页面横向溢出。

**地图正常路径实际通过**：CARTO 样式、sprite、字体、矢量瓦片及打开照片的原图请求成功，记录中无失败请求或页面异常。桌面/移动地图显示该选集的 16 张有坐标照片；点击“15”聚合展开为“14”聚合与独立点；点击独立点进入 `DSC_5331.jpg` Viewer。搜索 `DSC_5551` 后地图只剩对应单点和一条列表记录。桌面与移动端分别核对实际拍摄时间、焦距/快门及 GPS/海拔。

**独立 fixture**：补足真实数据缺少的无时区、不同偏移/跨日、无 EXIF、零/南西半球/越界 GPS、海平面以下及长曝光等情况。真实 MapLibre/WebGL 使用本地固定样式验证聚合、展开、点选、筛选及返回视角；该用例的字体为空 fixture，不能代替上述真实 CARTO 底图与数字标签验收。网络失败在可用 WebGL 下独立注入，GPU 全失效和模块下载失败另测。

完整 **`pnpm test` 退出码 0**（Node 24.19.0，Playwright 1.62.1 / Chromium 151.0.7922.34）：

| 检查 | 结果 |
| --- | --- |
| TypeScript strict | 通过 |
| Project System | 46/46 |
| Photo Engine 网络与处理 smoke | 通过 |
| Viewer 浏览器 bundle / worker 隔离 | 通过 |
| Website | 21 个浏览器用例 + 4 个元数据用例，25/25，无跳过 |
| 正式 Astro build | 通过，0 published / 0 draft |
| `git diff --check` | 通过 |

既有无 JavaScript 浏览、布局、触摸手势、排序筛选、加载/重试、GPU 降级、HDR 状态、分享直达/刷新/历史和焦点测试均保留并通过。测试前后正式 Project、Manifest、缩略图逐文件 SHA-256 一致。Manifest SHA-256 仍为 `c342c50d25c39f301d4a9c00a1b8824ba706717ab6bb5946f25bd5425e3187d6`。

## 未验证项与复现

- 未做 Safari/Firefox、实体手机或实体 HDR 屏幕亮度/色彩验收；本次为 Chromium 桌面和移动视口模拟。没有重新处理或逐张目视验收全部原图。
- 地图外部服务未来的可达性不在本次结论内。无时区日期不能据此确定绝对时刻；同坐标照片仍可通过地图下方列表逐张打开。分享/刷新不保留地图缩放和平移。
- 本机证据：`.cache/phase4-full-test.log`、`.cache/phase4-real-network.json`、`.cache/phase4-real-*.png`。真实网络验证脚本为本机临时 `.cache/phase4-inspect.mjs`；这些缓存不提交。
- 自动回归：`pnpm test`；仅本阶段网站/元数据用例：`pnpm test:website`。真实照片复查：`pnpm ui:preview` 后打开 `http://127.0.0.1:4324/`。新增数据与资产处理约定已同步到 `ARCHITECTURE.md`。
